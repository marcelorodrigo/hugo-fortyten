baseURL: 'https://fortyten.marcelorodrigo.com/'
languageCode: 'en-US'
title: 'FortyTen Theme'

params:

# Enable JSON output for search index (required when enable_site_search is true)
outputs:
  home:
    - HTML
    - RSS
    - JSON
